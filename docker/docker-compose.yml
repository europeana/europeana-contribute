version: "3"
services:
  minio:
    image: minio/minio
    ports:
    - "3001:9000"
    command: server /data
    volumes:
    - ../tmp/minio:/data
  mongodb:
    image: mongo
    ports:
    - "3002:27017"
  redis:
    image: bitnami/redis
    environment:
    - ALLOW_EMPTY_PASSWORD=yes
    ports:
    - "3003:6379"
    #volumes:
    #- ../tmp/redis:/bitnami
